@model Dictionary<string, string>

@{
    ViewData["Title"] = "Parus RX HealthCheck Configuration";
}

<h1>Configuration Values</h1>

<table>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Key</td>
            <td>@item.Value</td>
        </tr>
    }
</table>