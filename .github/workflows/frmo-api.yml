name: frmo-api

on:
  push:
    branches:
      - main
    paths:
      - src/Common/**
      - src/Services/Egisz/Frmo/**
      - .github/workflows/frmo-api.yml
  pull_request:
    branches:
      - main
    paths:
      - src/Common/**
      - src/Services/Egisz/Frmo/**
      - .github/workflows/frmo-api.yml

env:
  DOTNET_VERSION: '10.x'
  DOTNET_QUALITY: 'preview'
  REGISTRY_ENDPOINT: docker.io

jobs:
  build-for-pr-linux:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          dotnet-quality: ${{ env.DOTNET_QUALITY }}

      - name: Build
        run: dotnet build -c Release src/Services/Egisz/Frmo/Frmo.slnf

      - name: Test
        run: dotnet test -c Release src/Services/Egisz/Frmo/Frmo.slnf

  build-linux:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'pull_request' }}
    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ ENV.REGISTRY_ENDPOINT }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build
        run: dotnet build -c Release src/Services/Egisz/Frmo/Frmo.slnf

      - name: Test
        run: dotnet test -c Release src/Services/Egisz/Frmo/Frmo.slnf

      - name: Publish
        run: dotnet publish src/Services/Egisz/Frmo/Frmo.slnf -r linux-x64 -p PublishProfile=DefaultContainer -p ContainerRegistry=${{ ENV.REGISTRY_ENDPOINT }} -p ContainerImageTag=dev
