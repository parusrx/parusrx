name: 'dadata'

on:
  push:
    branches:
      - main
    paths:
      - 'src/Common/**'
      - 'src/Services/DaData/**'
      - '.github/workflows/composite/**'
      - '.github/workflows/dadata.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'src/Common/**'
      - 'src/Services/DaData/**'
      - '.github/workflows/composite/**'
      - '.github/workflows/dadata.yml'

env:
  SERVICE: 'prx-dadata'
  IMAGE_NAME: 'prx-dadata'

jobs:
  BuildLinux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: ./.github/workflows/composite/build-push
        with:
          service: ${{ env.SERVICE }}
          image_name: ${{ env.IMAGE_NAME }}
          registry_username: ${{ secrets.REGISTRY_USERNAME }}
          registry_password: ${{ secrets.REGISTRY_PASSWORD }}
